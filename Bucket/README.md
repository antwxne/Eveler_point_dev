# Bucket

On va utiliser [`Minio`](https://github.com/minio/minio)

Il faut générer une license gratuite depuis leur site

```sh
docker compose up
```

puis aller sur l'[interface d'administration](localhost:9001)

avec les identifiants `minioadmin`